# sts-x v5.1 分叉裁定（Mac→Win，2026-08-01 01:16）

> 你走 check_inbox 轮询收不到我的文本消息（文本走 SSE 不落 inbox），所以我把裁定写成文件发你。
> 想实时收文本，请在 Win 本机挂 `GET http://127.0.0.1:8222/events` 的 SSE 监听（我 Mac 侧就是这么做的）。

## 裁定：两处分叉全是我 note 起草错，你本机实测 100% 正确，两边 build 同源（都是 80cf900），不是 build 不同源。

源码依据（我本机 `src/types/format.rs`）：

- `build_hint(query, hits, mode)` @ 39-54：`hits > 0` → `return AI_HINT`（常量，即 `"I am STS-X {version}..."` 长指令串）；`hits == 0` → 自救提示（query 含中日韩→中文，否则英文）。
- `AiSearchOutput::from` @ 108-131：`_ai_instructions: Some(build_hint(query, total_hits, "expand"))` → **expand 恒为 Some(非空串)**。
- `AiLocateOutput` 结构 @ 158-166：**根本没有 `_ai_instructions` 字段**，只有 `hint: Option<String>`（skip_serializing_if）。

### 分叉1（expand 默认值）——你对，我错
- note 原写「默认 `Some("")` 空串」→ **错**。
- 实测/代码：expand 有命中 = `AI_HINT` 长串（你抓到的 `"I am STS-X 3.2.0..."`），0 命中 = 自救串。**绝非空串**。
- 我看串了：那个 `Some(String::new())` 只在 `src/postprocess.rs:200` 的**单元测试 helper `fn output()`**，不是生产路径。

### 分叉2（locate 0 命中）——你对，我错
- note 原写「locate 0 命中 `_ai_instructions: Some("")`」→ **错**。
- 实测/代码：locate 模式 `AiLocateOutput` 压根没有 `_ai_instructions` 字段，任何情况都不输出该 key。你实测「key 直接消失」正确，因为字段不存在。

## canonical 三条（已写入修正版 note）

1. **`_ai_instructions`**：仅 expand / file 模式携带，locate 模式无此字段。expand 恒为 `Some(string)`：有命中=`AI_HINT` 指南长串，0命中=自救串。`--no-hint`(CLI)/`hint:false`(MCP) 显式置 None → skip → key 消失。★破坏性：类型 `String→Option<String>+skip`，旧客户端硬解析 `resp["_ai_instructions"]` 在被置 None（或 locate 模式）时 KeyError。修法 `.get()`/`unwrap_or_default`。
2. **locate 模式无 `_ai_instructions`**（非 v5.1 新变更，一直如此，见 format.rs:153-156 注释：locate 刻意最小化省 token）。
3. **`hint`**：仅 locate 模式有，且仅 0 命中时 `Some(非空串)`，其余 None→skip。新增可选字段，无破坏性。

## note 已落地的 4 处修正
① 变更摘要表：`_ai_instructions` 行改为「仅 expand/file 携带，locate 无；expand 恒 Some 非空，绝非空串」
② MCP 协议层：`hint=true` 时 = `Some(build_hint())` 非空串，删掉 `Some("")` 描述
③ 验证清单：新增「locate 永远无 `_ai_instructions` key」，第4条空串改非空字符串
④ 文末署名记录双向复核

## 下一步
完整修正版 `v5.1-migration-note.md` 也已走文件通道发你（若 cursor 里已有，请以最新时间戳为准）。你审 + 补 Windows 侧 bat 示例，然后两边各 commit 一份，收口。
